vars: [n]
requirements: {n > 0 /\ n = 11}
<!=_=!>
length := 1;
while (n /= 1) ?{n > 0} {
      if (n % 2 = 0) {
         n := n / 2;
      } else {
         n := 3 * n + 1;
      };
      length := length + 1;
};
#{length = 15};

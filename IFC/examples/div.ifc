a := 10;
b := 8;
q := 0;
r := a;
-- 👻n := a;
-- #{a = 👻n};
#{0 <= a /\ 0 < b};
while r >= b
?{ a = q * b + r /\ 0 <= r}
!{r}
{
r := r - b;
q := q + 1;
};
#{exists v. q * b + v = a /\ 0 <= v /\ v < b};

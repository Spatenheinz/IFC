vars: [x]
requirements: {x >= 0}
<!=_=!>
y := 1;
k := 420;
while (x > 0 || y > 0)
?{true}
!{x, y}
{
    if x > 0 {
        x := x - 1;
        y := y + y;
    } else {
        y := y - 1;
    };
};
if x <= 0 && y <= 0 {
k := 42;
};
#{k = 42};

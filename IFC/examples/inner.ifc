vars: [x]
requirements: {x > 1}
<!=_=!>
👻n := x;
#{👻n >= 0 };
q := 0;
i := x;
p := 1;
while 2 * p <= x
?{2 * q + x = 👻n};
?{0 <= q};
?{0 <= x};
?{1 <= p};
?{2 <= i};
?{i = x + 2 * (p - 1)}
!{x}
{
q := q + p;
p := 2 * p;
x := x - p;
};
#{x < i};
